#include "parser/parser.h"

namespace expr {
    int parser::parse(const std::string &f) {
        return 0;
    }
    auto parser::get_symbol(const std::string &identifier) -> syntax_tree_t {
        return {};
    }
    void parser::set_symbol(const std::string &identifier, const symbol_value_t &value) {

    }
}
